# Third-party references

Morrow's InnerTube bridge was implemented with reference to public reverse-engineering work around YouTube's private InnerTube API, including:

- `tombulled/innertube` — MIT licensed
- `LuanRT/YouTube.js` — MIT licensed

Morrow does not bundle either library; the bridge is a small independent JavaScript implementation of the required requests and response parsing.
